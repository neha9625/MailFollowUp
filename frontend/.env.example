# Optional. Leave empty to use the Vite dev proxy (/api → http://localhost:5000).
# In production the Express server serves the built app from the same origin.
VITE_API_BASE_URL=http://localhost:5000

# Only needed if you enabled ADMIN_API_TOKEN in the backend .env
VITE_ADMIN_API_TOKEN=6b465b0d-e47a-4eff-91f5-ffa60f62fb28
